import { SlideViewMainContentDirective } from './slide-view-main-content.directive';

describe('SlideViewMainContentDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideViewMainContentDirective();
    expect(directive).toBeTruthy();
  });
});
