import { SlideViewCloseDirective } from './slide-view-close.directive';

describe('SlideViewCloseDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideViewCloseDirective();
    expect(directive).toBeTruthy();
  });
});
